{
  "name": "build-git",
  "version": "0.1.0",
  "description": "Building Git from scratch in TypeScript",
  "type": "module",
  "bin": {
    "mygit": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc",
    "prepack": "npm run build",
    "mygit": "tsx src/index.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
